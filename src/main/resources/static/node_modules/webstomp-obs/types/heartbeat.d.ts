import { ConnectedHeaders } from './headers';
export interface HeartbeatOptions {
    outgoing: number;
    incoming: number;
}
declare class Heartbeat {
    private heartbeatSettings;
    private pinger;
    private ponger;
    private lastServerActivity;
    private debug?;
    constructor(heartbeatSettings: HeartbeatOptions, debug?: (message: any, ...args: any[]) => void);
    startHeartbeat: (headers: ConnectedHeaders, callback: {
        send: (data: any) => any;
        close: Function;
    }) => void;
    stopHeartbeat: () => void;
    activityFromServer: () => void;
    private _startPinger;
    private _startPonger;
    private _debug;
}
export default Heartbeat;
